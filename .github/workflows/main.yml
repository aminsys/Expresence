name: Deploy to Github Page
on:
  push:
    branches: [ "main" ]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    #- name: Navigate to expresence folder and npm isntall
    #  run: |
    #    cd ./expresence
    #    echo "Current folder path: $PWD"
    #    ls
    #    npm install
    #- name: Install dependencies
    #  run: npm install
    #- name: Build project Expresence
    #  run: npm run build --prefix ./expresence

    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'aminsys'
        reponame: 'Expresence/expresence'
        token: ${{ secrets.GITHUB_TOKEN }}
